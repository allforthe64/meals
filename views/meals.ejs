<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="css/style.css">
</head>
<body>
    <div class="main">
        <h1>Your Saved Meals</h1>
        <div class="gallery-container">
            <% meals.forEach( el => { %>
                <div class="recipe-card">
                    <li class='mealItems' data-id='<%=el._id%>'>
                        <img class="recipe-image" src="<%= el.img %>" alt="food pic">
                        <h4 class='recipe-title'><%= el.meal %></h4>
                        <div class="link-con">
                            <a href="<%= el.link %>" target="blank">Link</a>
                        </div>
                        <span class='del'> Delete </span>
                    </li>
                </div>
            <% }) %>    
        </div>
        <div class="random">
            <h2>Can't decide? Get a random meal!</h2>
            <% if (selector !== -1) { %>
                <div class="recipe-card">
                    <li class='mealItems' data-id='<%=el._id%>'>
                        <img class="recipe-image" src="<%= meals[selector].img %>" alt="food pic">
                        <h4 class='recipe-title'><%= meals[selector].meal %></h4>
                        <div class="link-con">
                            <a href="<%= meals[selector].link %>" target="blank">Link</a>
                        </div>
                        <span class='del'> Delete </span>
                    </li>
                </div>
                <!-- <div>
                    <span class='<%= meals[selector].completed === true ? 'completed' : 'not'%>'><%= meals[selector].meal %></span>
                </div> -->
            <%}%>
        </div>
    </div>
    

    <form action="/meals/createMeal" method='POST'>
        <input type="text" placeholder="Enter Meal Item" name='mealItem'>
        <input type="url" placeholder="Enter Recipe Link" name='link'>
        <input type="url" placeholder="Enter Image URL" name='img'>
        <input type="submit">
    </form>

    <a href="/logout">Logout</a>

    <script src="js/main.js"></script>
</body>
</html>